services:
  - type: web
    name: your-app
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py  # Adjust to your actual start command
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
